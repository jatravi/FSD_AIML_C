const express = require ('express')
const app = express();
const PORT = 3000;

app.get('/',(req,res) => {
  res.send("TEST CASE")
});

app.listen(POST , (error) => {
  if (!errir) console.log('Server is running successfully on port', $(POST))
  else console.log("Server has not started")
});
